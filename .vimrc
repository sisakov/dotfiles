" humbers of lines
set number

" 5 lines on the top of the screen
set scrolloff=5

" highht ligth search
set hlsearch

" show the cursor position
set ruler

" show incoplete commands.
set showcmd

" show a menu when using tab completion
set wildmenu

" default history=50
set history=500

" backup with ~
set backup

" set outo indent
set ai
set si

" map Key Keystrokes

" syntax hight ligth
filetype plugin indent on
syntax on

" set colorschme to slate
colorscheme slate
